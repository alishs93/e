{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Auto Commit & Push",
            "type": "shell",
            "command": "$commitMessage = 'Saved'; git add .; git commit -m \"$commitMessage\"; git push origin main",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}